import fs from 'node:fs';
import path from 'node:path';
import {EvidenceStore,sha} from '../../plugins/uigates/learning/evidence.mjs';
const planId=process.argv[2]??'workboard-v1';
const dir=path.resolve('evaluations/real-project-v1');
const store=new EvidenceStore(path.join(dir, planId==='workboard-v1'?'store':`store-${planId.replace('workboard-v1-','')}`));
const certificate=store.get('certificates',planId),r=certificate.body;
const runs=store.all('runs').map(x=>x.body),fmt=n=>n===null||n===undefined?'unknown':n.toLocaleString('en-US');
const csv=['task,family,control_accepted,treatment_accepted,control_tokens,treatment_tokens,tokens_saved',...r.pairs.map(p=>[p.task,p.family,p.control?.quality??false,p.treatment?.quality??false,p.control?.usage.total??'',p.treatment?.usage.total??'',p.saved??''].join(','))].join('\n')+'\n';
fs.writeFileSync(path.join(dir,planId==='workboard-v1'?'results.csv':'results-complete.csv'),csv);
fs.writeFileSync(path.join(dir,planId==='workboard-v1'?'certificate.json':'certificate-complete.json'),JSON.stringify(certificate,null,2)+'\n');
const setup=['smoke.jsonl','smoke-clean.jsonl','smoke-authorized.jsonl','smoke-current.jsonl'].map(file=>({file,sha256:sha(fs.readFileSync(path.join(dir,file)))}));
fs.writeFileSync(path.join(dir,'setup-artifacts.json'),JSON.stringify(setup,null,2)+'\n');
const learning=r.learningSupported?'Supported within the frozen Workboard workload':'Not supported by this experiment';
const savings=r.tokenSavingsSupported?'Supported after observed operational overhead':'Not supported after observed operational overhead';
const cert=r.certified?'CERTIFIED WITHIN TESTED SCOPE':'NOT CERTIFIED';
const rows=r.pairs.map(p=>`| ${p.task} | ${p.family} | ${p.control?.quality?'pass':'fail/missing'} / ${p.treatment?.quality?'pass':'fail/missing'} | ${fmt(p.control?.usage.total)} | ${fmt(p.treatment?.usage.total)} | ${fmt(p.saved)} |`).join('\n');
const report=`# Workboard: real coding-agent evaluation\n\n**${cert}.** The full frozen experiment is retained, including negative results.\n\n- Learning/guidance effect: **${learning}**.\n- Net token savings: **${savings}**.\n- Unbounded general coding-agent learning: **not certified**.\n\nThis experiment used ${r.scope.model} at ${r.scope.reasoningEffort} effort through a compatible Codex CLI. Agents wrote real feature modules, ran local checks, and were accepted or rejected by a separately executed, hash-pinned verifier. No worker was given a menu of prewritten solutions. All features belong to the usable [Workboard CLI](../../examples/workboard/README.md).\n\nThe first execution attempt was interrupted by a Codex usage limit. The historical store retains every attempted arm, including unknown-token failures. A rerun through [resume.mjs](resume.mjs) was attempted afterwards into \`store-complete\` and failed at startup for an environment reason, not a learning result; no certificate is based on it. See [ATTEMPTS.md](ATTEMPTS.md).

## Actual token totals\n\n| Metric | Tokens |\n|---|---:|\n| Control arms | ${fmt(r.controlTokens)} |\n| Learned-guidance arms | ${fmt(r.treatmentTokens)} |\n| Observed learning overhead | ${fmt(r.observedOverheadTokens)} |\n| Net saved after overhead | ${fmt(r.netTokensSaved)} |\n\nPositive saved means treatment used fewer tokens. Full input plus output is counted; cached input is a subset of input and is not added again. Tokens are not billing dollars. The 95% paired-bootstrap net interval is **[${r.netSavings95CI.map(fmt).join(', ')}]**. Treatment acceptance's Wilson interval is **[${r.acceptance95CI.map(n=>(100*n).toFixed(1)+'%').join(', ')}]**, conditional on this workload, not an assurance about arbitrary future tasks.\n\n${r.scope.costScope}\n\nThe operational overhead includes both discovery tasks, the real synthesis model call, and the successful current-CLI smoke call. The failed environment preflights are preserved separately; missing preflight usage is not converted to zero and is outside this operational-cycle claim. This does **not** establish total token savings for the entire parent task, implementation of the framework, or creation of this benchmark.\n\n## Every predeclared pair\n\n| Feature | Family | Acceptance control / guidance | Control tokens | Guidance tokens | Saved |\n|---|---|---|---:|---:|---:|\n${rows}\n\n## What was learned\n\nThe candidate lesson was synthesized from two actual accepted discovery traces (add and complete). It identifies transaction, validation, cloning and persistence contracts and observed verification practices. It cites the original runs and hashes its source dependencies. The treatment received this compact text; the control received the same source and requirements without it. Source contracts were available to both arms, so any supported result is a repository-specific **guidance effect**, not proof of novel knowledge or model retraining. The candidate was authorized only for this bounded evaluation; no live principal approval was invented.\n\n## Experimental controls and limitations\n\n- Sixteen distinct feature tasks were fixed before holdout execution. Each used a new process and separate disposable workspace; task order alternated control/treatment.\n- All arms used the same model/settings, source snapshot, required behavior and timeout. No failed arm was overwritten or selectively retried.\n- No evaluation output was synthesized back into the candidate. Discovery, synthesis and per-arm full traces remain in the store.\n- The 16 tasks span query, reporting, metadata and workflow work in **one JavaScript project**. They are purposively selected and share infrastructure; intervals do not demonstrate cross-repository or cross-language reliability.\n- Verification is independent of the coding agent but was authored by the parent task author. This is not blinded external expert review.\n- Control workspaces contain no lesson store or other arm artifacts. Standard local sandbox read access is not a hardened container-level blinding boundary; workers were instructed to stay within their workspace.\n- Approval identities and the local evidence store trust the operator. This is a reproducible local attestation, not third-party accreditation.\n\n## Evidence and reproduction\n\n- [Frozen plan](plan.json), [learned candidate](lesson.json), [certificate](certificate.json), [all pair results CSV](results.csv).\n- Immutable records and content-addressed traces, check output and produced code: [store](store/).\n- [External verifier](verify.mjs), [task definitions](workload.mjs), [discovery runner](discover.mjs), [synthesis runner](synthesize.mjs).\n- Framework guidance: [evidence-backed learning](../../docs/certified-learning.md).\n\nA certificate cannot be overwritten. To replicate the experiment, use a **new** store and plan ID and record every arm again. The preserved original dataset is historical evidence, not a cache to rewrite until results improve.\n\n## Reasons the claim gate withheld support\n\n${r.reasons.length?r.reasons.map(s=>'- '+s).join('\n'):'No missing-data or acceptance reasons. Statistical and net-overhead gates still determine the reported support flags.'}\n\n## Measured run inventory\n\n${runs.map(x=>`- ${x.id}: ${x.accepted===undefined?'synthesis':x.accepted?'accepted':'not accepted'}; ${fmt(x.usage?.total)} tokens.`).join('\n')}\n`;
fs.writeFileSync(path.join(dir,planId==='workboard-v1'?'REPORT.md':'REPORT-COMPLETE.md'),report);
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const max=Math.max(...r.pairs.flatMap(p=>[p.control?.usage.total??0,p.treatment?.usage.total??0]),1);
const charts=r.pairs.map(p=>`<div class="chart"><b>${esc(p.task)}</b><div><i style="width:${100*(p.control?.usage.total??0)/max}%;background:#6e91af"></i><small>Control ${fmt(p.control?.usage.total)}</small></div><div><i style="width:${100*(p.treatment?.usage.total??0)/max}%;background:#dfad5f"></i><small>Guidance ${fmt(p.treatment?.usage.total)}</small></div></div>`).join('');
fs.writeFileSync(path.join(dir,planId==='workboard-v1'?'index.html':'index-complete.html'),`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>UI-GATES · Workboard evidence</title><style>body{background:#0d1420;color:#e8edf3;font:16px system-ui;margin:0}main{max-width:1050px;margin:auto;padding:40px 24px}h1{font-size:38px;margin:12px 0}p{line-height:1.65;color:#b5c2d0}.tag{font-size:12px;letter-spacing:2px;color:#dfad5f}.cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(210px,1fr));gap:16px;margin:30px 0}.card{padding:20px;background:#172333;border:1px solid #2a3d52;border-radius:12px}.card b{display:block;font-size:28px;margin-top:10px}.chart{padding:18px 0;border-bottom:1px solid #293749}.chart div{display:flex;align-items:center;gap:10px;height:25px}.chart i{display:inline-block;height:10px;max-width:70%;border-radius:3px}.chart small{white-space:nowrap;color:#bac8d6}a{color:#9dceef}footer{margin-top:35px;color:#93a4b7;line-height:1.6}</style><main><div class="tag">UI-GATES / REAL CODING-AGENT EVIDENCE</div><h1>${cert}</h1><p>Sixteen Workboard features. Thirty-two coding runs. One frozen lesson from two verified discovery tasks. Full telemetry and independent behavioral checks.</p><div class="cards"><div class="card">Control tokens<b>${fmt(r.controlTokens)}</b></div><div class="card">Guidance tokens<b>${fmt(r.treatmentTokens)}</b></div><div class="card">Learning overhead<b>${fmt(r.observedOverheadTokens)}</b></div><div class="card">Net tokens saved<b>${fmt(r.netTokensSaved)}</b></div></div><p><strong>Learning effect:</strong> ${learning}.<br><strong>Net savings:</strong> ${savings}.<br><strong>General coding-agent reliability:</strong> this single-project experiment does not establish it.</p><p><a href="REPORT.md">Full report</a> · <a href="results.csv">Raw CSV</a> · <a href="certificate.json">Certificate JSON</a> · <a href="../../examples/workboard/README.md">Workboard project</a></p><h2>Every paired task</h2>${charts}<footer>Input + output tokens; cached input counted once. Net savings includes observed operational learning overhead, excluding one-time framework and benchmark development. No billing claim. Data is the complete frozen experiment, including unsuccessful outcomes.</footer></main></html>`);
console.log(JSON.stringify({status:r.status,control:r.controlTokens,treatment:r.treatmentTokens,overhead:r.observedOverheadTokens,net:r.netTokensSaved,learningSupported:r.learningSupported,tokenSavingsSupported:r.tokenSavingsSupported},null,2));
