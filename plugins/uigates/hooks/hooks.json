{
  "description": "UI-GATES write-time gate. Refuses a file edit that no unspent authorization covers. Does nothing unless enforcement is on (`uigates enforce on` or UIGATES_ENFORCE=1), fails open on any error, and sees only the file-editing tools, not writes made through Bash.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "npx --no-install uigates hook pre-write",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
