# Copy to .env (gitignored) and fill in real values there. Never commit .env.
#
# Required only for `uigates advise` with UIGATES_JEV_BACKEND=typesafe
# (docs/compound-engineering/graph-jev-aar.md in the gerardoiornelas-portfolio repo).
# Without it, `uigates advise` uses the local stub backend and needs nothing here.
TYPESAFE_API_KEY=
